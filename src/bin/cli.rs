fn main() {}

/*
            let mut cli = match cli::Cli::new(config_path) {
                Ok(cli) => cli,
                Err(e) => {
                    println!("{}", e);
                    return;
                }
            };

            // Play
            if let Err(e) = cli.run() {
                eprintln!("{}", e);
            }
*/
